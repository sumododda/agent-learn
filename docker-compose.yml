services:
  db:
    image: postgres:16
    ports:
      - "127.0.0.1:5432:5432"
    environment:
      POSTGRES_USER: agentlearn
      POSTGRES_PASSWORD: agentlearn
      POSTGRES_DB: agentlearn
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
